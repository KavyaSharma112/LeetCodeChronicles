import ReactDOM from "react-dom/client";
import React from "react";

const heading = <h1>This is LeetCode Chronicles !</h1>

const root = ReactDOM.createRoot(document.getElementById("intro"));
root.render(heading);